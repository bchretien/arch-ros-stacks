diff -Naur kdl-1.0.2-orig/config/FindSIPandPython.cmake kdl-1.0.2/config/FindSIPandPython.cmake
--- kdl-1.0.2-orig/config/FindSIPandPython.cmake	2009-09-03 15:19:35.000000000 +0200
+++ kdl-1.0.2/config/FindSIPandPython.cmake	2012-04-19 19:32:04.271168513 +0200
@@ -19,10 +19,10 @@
   IF (SIP_FOUND)
     # check for SIP version
     # minimal version is 4.4
-    SET (SIP_MIN_VERSION 040709)
-    IF (SIP_VERSION EQUAL "${SIP_MIN_VERSION}" OR SIP_VERSION GREATER "${SIP_MIN_VERSION}")
+    SET (SIP_MIN_VERSION 4.7.9)
+    IF (SIP_VERSION_STR VERSION_EQUAL SIP_MIN_VERSION OR SIP_VERSION_STR VERSION_GREATER SIP_MIN_VERSION)
       SET (SIP_IS_GOOD TRUE)
-    ENDIF (SIP_VERSION EQUAL "${SIP_MIN_VERSION}" OR SIP_VERSION GREATER "${SIP_MIN_VERSION}")
+    ENDIF (SIP_VERSION_STR VERSION_EQUAL SIP_MIN_VERSION OR SIP_VERSION_STR VERSION_GREATER SIP_MIN_VERSION)
     
     IF (NOT SIP_IS_GOOD)
       MESSAGE (STATUS "SIP is required in version 4.7.9 or later!")
@@ -40,4 +40,4 @@
     MESSAGE(STATUS "Python bindings disabled due dependency problems!")
   ENDIF (SIP_IS_GOOD)
   
-ENDIF (PYTHON_LIBRARIES AND PYTHON_INCLUDE_PATH)
\ No newline at end of file
+ENDIF (PYTHON_LIBRARIES AND PYTHON_INCLUDE_PATH)
